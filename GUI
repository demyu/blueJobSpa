import java.awt.*;
import java.awt.event.*;
import javax.swing.*;

public class calendarGUI extends JFrame{
    
    private JTextField tbName, tbPass, tbDate;
    private JButton bLogin, bAdd, bAccept, bClear;
    private JComboBox services;
    private JPanel login1, login2, login3, login4;
    private JFrame r;
    private JLabel total;
    public calendarGUI(){

        tbName = new JTextField("Input Name");
            tbName.setFont(new Font("Luciana", Font.BOLD, 20));
            tbName.setHorizontalAlignment(JTextField.CENTER);
        tbPass = new JTextField("");
            tbPass.setFont(new Font("Luciana", Font.BOLD, 20));
            tbPass.setHorizontalAlignment(JTextField.CENTER);
        tbDate = new JTextField("mm/dd/yy");
            tbDate.setFont(new Font("Luciana", Font.BOLD, 20));
            tbDate.setHorizontalAlignment(JTextField.CENTER);
        bLogin = new JButton("Login");
        bAdd = new JButton("Add");
        bAccept = new JButton("Reserve");
        bClear = new JButton("Clear");
        services = new JComboBox();
        total = new JLabel("0");
            total.setFont(new Font("Luciana", Font.BOLD, 20));
            total.setHorizontalAlignment(JTextField.CENTER);
        
        login1 = new JPanel(new GridLayout(3,1));
        login2 = new JPanel(new GridLayout(3,2));
        login3 = new JPanel(new FlowLayout());
        login4 = new JPanel(new FlowLayout());
        
        setLayout(new GridLayout(3,2));
        
        add(login1, BorderLayout.EAST);
        add(login2, BorderLayout.CENTER);
        add(login3, BorderLayout.CENTER);
        
        login1.add(tbName);
        login1.add(tbPass);
        login1.add(bLogin);
        
        login2.add(tbDate);
        login2.add(services);
        login2.add(total);
        login2.add(bAdd);
        
        login3.add(bAccept, BorderLayout.CENTER);
        login3.add(bClear, BorderLayout.CENTER);
    }
}
